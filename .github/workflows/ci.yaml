name: CI

on:
  push:
    branches:
      - develop
      - master
    paths:
      - "workflow/**"
      - "config/**"
      - ".tests/**"
      - "requirements*.txt"
      - ".github/workflows/**"
      - ".pre-commit-config.yaml"
  pull_request:
    branches:
      - develop
      - master
    paths:
      - "workflow/**"
      - "config/**"
      - ".tests/**"
      - "requirements*.txt"
      - ".github/workflows/**"
      - ".pre-commit-config.yaml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint workflow
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements.test.txt
      - name: Install dependencies
        run: pip install -r requirements.txt -r requirements.test.txt
      - name: Lint
        working-directory: .tests/integration
        run: snakemake --lint -n -s ../../workflow/Snakefile --configfile config.yaml

  snakefmt:
    name: Snakefmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Lint Code Base
        uses: github/super-linter@v7
        env:
          VALIDATE_ALL_CODEBASE: false
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_SNAKEMAKE_SNAKEFMT: true
          OUTPUT_FOLDER: .github/linters/report

  dry-run:
    name: Snakemake dry run
    runs-on: ubuntu-latest
    needs: [lint, snakefmt]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Dry run — merge
        working-directory: .tests/integration
        run: snakemake -n -s ../../workflow/Snakefile --configfile config.yaml
      - name: Dry run — fastp + merge
        working-directory: .tests/integration
        run: snakemake -n -s ../../workflow/Snakefile --configfiles config.yaml configs/fastp_merge_config.yaml
      - name: Dry run — PACBIO
        working-directory: .tests/integration
        run: snakemake -n -s ../../workflow/Snakefile --configfiles config.yaml config_pacbio.yaml

  integration:
    name: Integration test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: [dry-run]
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Install Apptainer
        uses: eWaterCycle/setup-apptainer@v2
        with:
          apptainer-version: 1.4.5
      - name: Cache container images
        id: cache-apptainer
        uses: actions/cache@v4
        with:
          path: ~/singularity_containers
          key: ${{ runner.os }}-apptainer-${{ hashFiles('config/**/*.yaml', '.tests/integration/*.yaml', 'workflow/Snakefile') }}
          restore-keys: |
            ${{ runner.os }}-apptainer
      - name: Create singularity containers dir
        run: mkdir -p ~/singularity_containers
      - name: Integration test — merge
        working-directory: .tests/integration
        run: |
          snakemake -s ../../workflow/Snakefile -j 4 --show-failed-logs \
            --configfile config.yaml \
            --software-deployment-method apptainer \
            --apptainer-args "--cleanenv --bind /home/runner" \
            --apptainer-prefix ~/singularity_containers/ && rm -r prealignment
      - name: Integration test — fastp + merge
        working-directory: .tests/integration
        run: |
          snakemake -s ../../workflow/Snakefile -j 4 --show-failed-logs \
            --configfiles config.yaml configs/fastp_merge_config.yaml \
            --software-deployment-method apptainer \
            --apptainer-args "--cleanenv --bind /home/runner" \
            --apptainer-prefix ~/singularity_containers/ && rm -r prealignment
      - name: Integration test — PACBIO
        working-directory: .tests/integration
        run: |
          snakemake -s ../../workflow/Snakefile -j 4 --show-failed-logs \
            --configfiles config.yaml config_pacbio.yaml \
            --software-deployment-method apptainer \
            --apptainer-args "--cleanenv --bind /home/runner" \
            --apptainer-prefix ~/singularity_containers/ && rm -r prealignment
      - name: Integration test — subsample
        working-directory: .tests/integration
        run: |
          snakemake -s ../../workflow/Snakefile -j 4 --show-failed-logs \
            --configfiles config.yaml configs/config_subsample.yaml \
            --software-deployment-method apptainer \
            --apptainer-args "--cleanenv --bind /home/runner" \
            --apptainer-prefix ~/singularity_containers/ && rm -r prealignment
